class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        seen = set()
        # T:O(n) & S:O(n)
        for num in nums:
            if num in seen:
                return num

            else:
                seen.add(num)

        return -1
